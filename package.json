{
  "name": "skyhub",
  "version": "0.1.0",
  "private": true,
  "description": "Integração Mercado Livre <-> Sankhya da Skyline (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir .dist",
    "typecheck": "tsc -p .",
    "test": "node --test \"test/*.test.ts\""
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.139.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
